[project]
name = "piewall"
version = "0.2.1"
description = "Manage Windows Firewall rules from a simple window or the command line"
readme = "README.md"
license = "MIT"
authors = [
    { name = "theeraphat.ch", email = "theeraphatchueanokkhum@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "darkdetect>=0.8.0",
    "mcp>=2.2,<3",
    "psutil>=7.2.2",
    "pywin32>=312; sys_platform == 'win32'",
    "sv-ttk>=2.6.1",
]

[project.scripts]
piewall = "piewall.cli:main"
piewall-mcp = "piewall.mcp_server:main"

[project.gui-scripts]
piewall-gui = "piewall.gui:main"

[build-system]
requires = ["uv_build>=0.11.8,<0.12.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "pyinstaller>=6.22.3",
    "pytest>=9.1.1",
]
[tool.pytest.ini_options]
pythonpath = ["tests"]
